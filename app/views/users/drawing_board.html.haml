.col-100
  %h1.title (back to the) Drawing board
  %h4 Where a user can see their feeds and messages.

.col-22
  %h3='All subscriptions'
  %ul
    - @feeds.each do |feed|
      %li= link_to feed.title, feed
      = link_to 'Unsubscribe', "/feeds/#{feed.id}/subscriptions", method: :delete
      / = feed.notecount
  %h4='All Notifications'
  = "#{@user.notifications.size} new"


.col-58
  %h1 Some image
  %br

.col-20
  %h3=link_to 'Inbox (messages list)', messages_path
  / =render "messages/conversation"
  %h3='Tweets'
